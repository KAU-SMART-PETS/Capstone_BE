spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
kakao:
  rest-api-key: test
  redirect-uri: test
  admin-key: test

naver:
  client-id: test
  client-secret: test
  redirect-uri: test


cloud:
  aws:
    s3:
      bucket: testBucket
    stack.auto: false
    region.static: testRegion
    credentials:
      accessKey: testAccessKey
      secretKey: testSecretKey
